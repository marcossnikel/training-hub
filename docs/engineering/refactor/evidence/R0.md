# R0 — Restore a trustworthy tooling baseline

Task: R0
Base commit: c3eab67ad8286a337b0d1e3032ab87f257acfb26
Implementation commits: f247ef86528e7258447d7ca4659b046a6bc34d4b, 4289e9b40f7ec1e478bb6ab3705fc5e668c27589
Builder: gpt-5.6-terra, medium reasoning
Self-review: complete
Orchestrator correction cycles: 1
Independent review: deferred
Accepted by: milestone orchestrator

## Completion record

Task: R0 — Restore a trustworthy tooling baseline

Outcome: Biome is the sole lint command with its recommended preset active;
Prettier remains the sole formatter; a clean install and every required non-E2E
quality check pass.

Files changed:

- Tooling: `biome.json`, `package.json`, `package-lock.json`, and removal of
  `eslint.config.mjs`.
- Tooling guard: `src/lib/retired-coach.test.ts`.
- Authorized behavior-preserving lint repairs:
  `src/app/api/strava/callback/route.ts`, `src/lib/strava-byo.ts`,
  `src/lib/strava.ts`, and `src/components/totals-table.tsx`.

Protected invariants:

- OAuth owner resolution, state consumption, redirects, scope normalization,
  and Strava parsing are unchanged.
- ASCII control-character validation remains equivalent to the previous regex
  checks.
- No lint rule is disabled; React and Next domains and the recommended preset
  remain active.
- Prettier remains the formatter; Biome formatter and assist are disabled.
- No route contract, product behavior, data, auth, environment, provider
  effect, or UI output changed.

Automated checks:

- `npm ci`: pass.
- `npm run check:env`: pass.
- `npm run typecheck`: pass.
- `npm run lint`: pass; 236 warnings and 3 informational diagnostics remain
  visible, with no rule disabled.
- `npm run format:check`: pass.
- `npm run test:unit`: pass; 74 files and 714 tests.
- `npm run deadcode`: pass. Non-blocking configuration hints: the existing
  `src/lib/sports.ts` and `src/lib/db/**` Knip ignores may be removable, and
  compiled `.css` is not followed through imports.
- `npm run cycles`: pass; no circular dependencies.

Manual/browser proof: not applicable.

Compatibility removed/remaining: ESLint configuration and direct dependencies
were removed; no compatibility lint path remains.

Data/auth/environment/external impact: none.

Existing unrelated changes preserved: the orchestrator-owned roadmap and R0
packet updates remained unstaged during builder commits and are included only
in the acceptance state commit.

Self-review:

- Packet criteria reread: complete.
- Complete diff/status/new/deleted files inspected: complete.
- `git diff --check`: pass.
- Scope/non-goals/compatibility checked: complete.
- Security/data/environment/provider effects checked: complete.
- Skipped or unexercised proof: none of the required R0 checks.
- Repairs made during self-review: enabled the recommended preset, aligned
  Biome 2.5.10, and repaired the ten authorized diagnostics; all eight checks
  were rerun.

Remaining blocker or next task: none for R0; R1 is eligible but was not started.
